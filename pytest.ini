# pytest configuration file

[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Show print statements
addopts = -v -s --tb=short

# Coverage options (if using pytest-cov)
# addopts = -v -s --tb=short --cov=. --cov-report=html
